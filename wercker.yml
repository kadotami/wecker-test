box: python:2.7.16

build:
  steps:
    - script:
      name: show version
      code: python --version

box: python:3.6

build:
  steps:
    - script:
      name: show version
      code: python --version